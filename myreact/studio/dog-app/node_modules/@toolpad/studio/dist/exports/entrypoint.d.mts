import * as React from 'react';
import { ToolpadComponents } from '@toolpad/studio-runtime';
import { R as RuntimeState } from './types-CI_VLzfr.mjs';
import { T as ToolpadAppProps } from './ToolpadApp-CF8cGzdf.mjs';
import '@toolpad/studio-runtime/appDom';

/**
 * This allows us to hot update the components when a file is added/removed
 */
declare function setComponents(newComponents: ToolpadComponents): void;
interface InitParams {
    initialState: RuntimeState;
    base: string;
    ToolpadApp?: React.ComponentType<ToolpadAppProps>;
}
declare function init({ ToolpadApp, initialState, base }: InitParams): void;

export { type InitParams, init, setComponents };
