import * as React from 'react';
import { QueryClient } from '@tanstack/react-query';
import { ToolpadPlan } from './appDom';
export declare const queryClient: QueryClient;
export interface AppHost {
    isPreview: boolean;
    isCanvas: boolean;
    plan: ToolpadPlan;
}
export declare const AppHostContext: React.Context<AppHost | null>;
export interface AppHostProviderProps extends Partial<AppHost> {
    children?: React.ReactNode;
}
export declare function AppHostProvider({ isPreview, isCanvas, plan, children, }: AppHostProviderProps): React.JSX.Element;
export declare function useAppHost(): AppHost;
