import { TextFieldProps as MuiTextFieldProps } from '@mui/material';
import { FormInputComponentProps } from './Form';
interface FullFile {
    name: string;
    type: string;
    size: number;
    base64: null | string;
}
export type FilePickerProps = MuiTextFieldProps & {
    multiple: boolean;
    value: FullFile[];
    onChange: (files: FullFile[]) => void;
    label?: string;
} & Pick<FormInputComponentProps, 'name' | 'isRequired'>;
declare const _default: import("packages/toolpad-studio-runtime/dist").ToolpadComponent<FilePickerProps>;
export default _default;
