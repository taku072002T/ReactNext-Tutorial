import { BoxProps } from '@mui/material';
type ChartDataSeriesKind = 'line' | 'bar' | 'area' | 'scatter';
export declare const CHART_DATA_SERIES_KINDS: ChartDataSeriesKind[];
export interface ChartDataSeries<D = Record<string, string | number>> {
    kind: ChartDataSeriesKind;
    label: string;
    data?: D[];
    xKey?: keyof D;
    yKey?: keyof D;
    color?: string;
}
export type ChartData = ChartDataSeries[];
interface ChartProps extends BoxProps {
    data?: ChartData;
    loading?: boolean;
    error?: Error | string;
}
declare const _default: import("packages/toolpad-studio-runtime/dist").ToolpadComponent<ChartProps>;
export default _default;
