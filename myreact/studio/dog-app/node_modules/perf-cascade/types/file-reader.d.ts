import { Har } from "har-format";
/** handle client side file upload */
export declare function readFile(file: File, fileName: string, callback: (e: Error | null, har?: Har) => void, onProgress?: (progress: number) => void): void;
