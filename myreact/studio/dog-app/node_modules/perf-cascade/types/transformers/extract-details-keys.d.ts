import { Entry } from "har-format";
import { SafeKvTuple } from "../typing/waterfall";
/**
 * Data to show in overlay tabs
 * @param  {number} requestID - request number
 * @param  {WaterfallEntry} entry
 */
export declare function getKeys(entry: Entry, requestID: number, startRelative: number, endRelative: number): {
    general: SafeKvTuple[];
    request: SafeKvTuple[];
    requestHeaders: SafeKvTuple[];
    response: SafeKvTuple[];
    responseHeaders: SafeKvTuple[];
    timings: SafeKvTuple[];
};
